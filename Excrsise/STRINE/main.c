#include <stdio.h>
#include <stdlib.h>
#include<string.h>

int main()
{
    int i,ch;

    char n[10];
    scanf("%c",&ch);
    /*scanf("%s",&n);
    printf("%s",n);
printf("%s",n[10]);

    for(i=0;i<10;i++)
        {
            printf("%c",n[i]);
        }
        */

    return 0;
}
